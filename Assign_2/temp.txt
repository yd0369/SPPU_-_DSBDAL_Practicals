# -*- coding: utf-8 -*-
"""Student Performance.ipynb

Automatically generated by Colaboratory.

Original file is located at
https://colab.research.google.com/drive/1UkhY654xiaC1VNtP9gzZMCXZOTiWTCjG
"""

import pandas as pd

df = pd.read_csv('Student_Performance_Dataset.csv')
df.head()

df.tail()

df.shape

# It gives information
# 1) rows, columns count
# 2) column names
# 3) Data type of attributes

df.info()

# It gives count of how many missing values (Null values) are present
# In python null value or missing value = None
df.isna().sum()

df['gender'][0] = 'M'

df.isna().sum()

# After scanning we found inconsistency in column names
# Let us correct the column names

df = df.rename(columns={'gender':'Gender', 'NationalITy':'Nationality', 'raisedhands':'RaisedHands'})

df = df.rename(columns={'ParentschoolSatisfaction':'ParentSchoolSatisfaction'})

df.columns

Outlier :
Data value which is too high or too low
How to find it?

Box plot

df.info()

df.boxplot(column =['RaisedHands', 'VisITedResources', 'AnnouncementsView', 'Discussion'], grid = False)

# We found that there is no outlier

import seaborn as sns
sns.set_theme(style="whitegrid")
ax = sns.boxplot(x=df['RaisedHands'])

df['RaisedHands'][0] = 500

import seaborn as sns
sns.set_theme(style="whitegrid")
ax = sns.boxplot(x=df['RaisedHands'])

If there is outlier, how to remove it?
-> 1) some value which is beyond the range
2) some error in entering the data
3) Some knowledge

print("Highest allowed",df['RaisedHands'].mean() + 3*df['RaisedHands'].std())
print("Lowest allowed",df['RaisedHands'].mean() - 3*df['RaisedHands'].std())

df[(df['RaisedHands'] > 158 )]

df = df[(df['RaisedHands'] <= 158)]

import seaborn as sns
sns.set_theme(style="whitegrid")
ax = sns.boxplot(x=df['RaisedHands'])